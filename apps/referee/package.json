{
  "name": "@bmb/referee",
  "version": "0.1.0",
  "private": true,
  "description": "Trusted worker: fault injector, synthetic-user probes, mitigation oracle, diagnosis judge, and the constrained healer runner.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "biome check .",
    "test": "vitest run",
    "probes": "tsx src/cli/probes.ts",
    "seed": "tsx src/cli/seed.ts",
    "reset": "tsx src/cli/reset.ts",
    "test:faults": "vitest run test/faults.test.ts",
    "heal": "tsx src/cli/heal.ts",
    "test:unit": "vitest run test/isolation.test.ts test/healer-denial.test.ts",
    "eval": "tsx src/cli/eval.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.127.0",
    "@bmb/shared": "workspace:*",
    "@insforge/cli": "0.2.8",
    "@insforge/sdk": "^1.5.2",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.23.15",
    "typescript": "5.9.3",
    "vitest": "^4.1.11"
  }
}
