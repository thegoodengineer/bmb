/**
 * `pnpm --filter @bmb/referee probes` — runs the synthetic-user probe suite against the
 * victim and prints per-probe status and latency. Implemented in Phase 1.
 */
console.log('probes: not implemented yet (Phase 1)');
process.exitCode = 1;
