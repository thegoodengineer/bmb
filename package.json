{
  "name": "break-my-backend",
  "version": "0.1.0",
  "private": true,
  "description": "A public, live, adversarial mini-benchmark for agent-native backends, built on InsForge.",
  "license": "MIT",
  "packageManager": "pnpm@12.3.4",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "test": "pnpm -r test",
    "docs:catalog": "pnpm --filter @bmb/shared build && node packages/shared/scripts/gen-catalog.mjs",
    "web:package": "pnpm --filter @bmb/shared build && node scripts/build-web-deploy.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.14",
    "typescript": "5.9.3"
  }
}
